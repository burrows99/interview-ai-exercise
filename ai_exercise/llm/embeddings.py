"""Embedding function helpers — the active function is provided by the configured LLMProvider."""

# The embedding function is now accessed via `ai_exercise.constants.llm_provider.embedding_function`.
# This module is kept for backward compatibility.
